<main class="pb-24">
    <section class="bg-royal/10 pt-12 xl:pt-32 py-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 xl:gap-20">
                <div class="xl:pr-12">
                    <div class="mb-8">
                        <h1 class="font-display text-4xl md:text-5xl font-bold mb-3">{{this.title}}</h1>
                        <div class="prose prose-slate max-w-none">{{this.body}}</div>
                    </div>
                </div>
                <div class="lg:-mb-36">
                    <div class="bg-white shadow-sm rounded-2xl">
                        <div class="p-6">
                            {{include /components/forms/zoho-form.html}}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    {{blocks('/-/block/client_logos.html')}}
</main>
