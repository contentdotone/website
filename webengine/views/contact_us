<main class="pb-8">
    <!--Pageheader start-->
    <section class="bg-primary bg-opacity-10 pt-xl-9 pb-xl-0 py-5">
        <div class="container">
            <div class="row g-xl-7 gy-5">
                <div class="col-lg-6 col-12">
                    <div class="me-xl-7">
                        <div class="mb-5">
                            <h1 class="mb-3">{{this.title}}</h1>
                            <div>{{this.body}}</div>
                        </div>
                      
                    </div>
                </div>
                <div class="col-lg-6 col-12 mb-lg-n9">
                    <div class="card shadow-sm">
                        <div class="card-body">
                           {{include /components/forms/zoho-form.html}}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    {{blocks('/-/block/client_logos.html')}}

    <!--Pageheader end-->
</main>